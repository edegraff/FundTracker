@model Common.Models.Report

@* 3.2.1.1.2 Visitors and Users will be able to compare mutual funds side by side. (This view is used to compare multiple funds) *@
@* 3.2.1.1.4 Visitors and Users will be able to see informative graphs about the performance of a fund. (the custom report view allows users to see the performance of funds during specified periods of time) *@
@* 3.2.1.5.1 Predictions about the future performance/value of funds will be available, using existing prediction algorithms.  (If users selected a date into the future for their custom report they can view the predictions here.)*@

@{
    ViewBag.Title = "Report";
}

@if (@ViewBag.graph) {
    ViewBag.GraphOptions = true;
    @Html.Partial("_GraphPartial", Model);
}

@Html.Partial("_ExportPartial", Model)

@Html.Partial("_TablePartial", Model)

