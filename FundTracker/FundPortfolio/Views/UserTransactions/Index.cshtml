@model IEnumerable<Common.Models.UserTransaction>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FundEntity.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserProfile.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FundEntity.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserProfile.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserTransactionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserTransactionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserTransactionId })
        </td>
    </tr>
}

</table>
